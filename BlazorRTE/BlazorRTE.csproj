<Project Sdk="Microsoft.NET.Sdk.Razor">

	<PropertyGroup>
		<TargetFramework>net10.0</TargetFramework>
		<Nullable>enable</Nullable>
		<ImplicitUsings>enable</ImplicitUsings>

		<!-- NuGet Package Metadata -->
		<PackageId>BlazorRTE</PackageId>
		<Version>1.1.1</Version>
		<Authors>simscon1</Authors>
		<Company>LoneWorx LLC</Company>
		<Product>BlazorRTE</Product>
		<Description>Professional rich text editor for Blazor with emoji support. Native Blazor component with 53 features, 1800+ searchable emojis, 27 keyboard shortcuts, zero JavaScript dependencies, 100 percent test coverage, professional SVG icons, built-in XSS protection, and dark mode support. GPL v3 for open-source, commercial license coming soon.</Description>
		<PackageTags>blazor;rich-text-editor;wysiwyg;rte;editor;text-editor;emoji;emoji-picker;emoji-autocomplete;formatting;blazor-component;razor;contenteditable;gpl;accessibility;wcag;keyboard-shortcuts;testing;bunit</PackageTags>
		<PackageProjectUrl>https://github.com/simscon1/BlazorRTE</PackageProjectUrl>
		<RepositoryUrl>https://github.com/simscon1/BlazorRTE</RepositoryUrl>
		<RepositoryType>git</RepositoryType>
		<PackageLicenseExpression>GPL-3.0-or-later</PackageLicenseExpression>
		<PackageReadmeFile>README.md</PackageReadmeFile>
		<GeneratePackageOnBuild>false</GeneratePackageOnBuild>
		<PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
		<Copyright>Copyright (c) 2024-2026 LoneWorx LLC</Copyright>
		<PackageReleaseNotes><![CDATA[v1.1.1 Emoji Support, Testing and Bug Fixes (2026-02-06)

ADDED:
- Emoji Picker with 1800+ searchable emojis
- Emoji Autocomplete (type :smile for suggestions)
- 27 keyboard shortcuts (Ctrl+B, Ctrl+I, Ctrl+Shift+E, etc.)
- 54 unit tests with 100% pass rate (bUnit + xUnit)
- Voice control support (WCAG 2.5.3)

FIXED:
- Toolbar button click handlers now work reliably
- Emoji autocomplete positioning (viewport coordinates)
- EventsDemo page re-rendering issues
- All 54 tests passing

See full changelog: https://github.com/simscon1/BlazorRTE/blob/master/Docs/CHANGELOG.md]]></PackageReleaseNotes>
		
		<PackageIcon>icon.png</PackageIcon>
		<IncludeSymbols>true</IncludeSymbols>
		<SymbolPackageFormat>snupkg</SymbolPackageFormat>
		<EmbedUntrackedSources>true</EmbedUntrackedSources>
		<PublishRepositoryUrl>true</PublishRepositoryUrl>
	</PropertyGroup>

	<ItemGroup>
	  <Content Include="icon.png">
	    <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
	    <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
	  </Content>
	</ItemGroup>

	<ItemGroup>
		<SupportedPlatform Include="browser" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.AspNetCore.Components.Web" Version="10.0.2" />
	</ItemGroup>

	<!-- Include README and LICENSE in NuGet package -->
	<ItemGroup>
		<None Include="README.md" Pack="true" PackagePath="\" />
		<None Include="LICENSE.txt" Pack="true" PackagePath="\" />
		<None Include="icon.png" Pack="true" PackagePath="\" />
	</ItemGroup>
	<ItemGroup>
	  <ProjectReference Include="..\..\BlazorEmo.Solution\BlazorEmo\BlazorEmo.csproj" />
	</ItemGroup>
</Project>
